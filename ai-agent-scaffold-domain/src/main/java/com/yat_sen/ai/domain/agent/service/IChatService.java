package com.yat_sen.ai.domain.agent.service;

/**
 * 智能体对话服务接口（占位）。
 * <p>
 * 与 {@link IArmoryService}（装配）相对：装配负责"把配置建成智能体"，
 * 本服务负责"调用已装配好的智能体进行对话/执行"。
 * TODO 待定义对话入口（如 chat(请求) -> 响应）。
 */
public interface IChatService {
}

